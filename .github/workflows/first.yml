name: First

on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3



          - name: run tests
            run: ./gradlew


          - name: build project
            run: ./gradle assemble